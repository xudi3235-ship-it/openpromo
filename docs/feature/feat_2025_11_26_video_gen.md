feature:
we have connect rpc setup for video generation in modal. and we can call it via `rpc.ts`. i want you to scaffold a labs route called video-gen, and scaffold some UI, a orpc internal router which calls rpc to generate video. and polls until video's generated and play the video in the client side.

you need to refer to the image gen in labs route. and insepect the protos for what are the requests params for the video gen.


