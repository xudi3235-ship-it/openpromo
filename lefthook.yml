pre-commit:
  commands:
    check:
      run: echo "enable after migration to wrangler"
      # run: pnpm exec biome check --write --diagnostic-level=warn --error-on-warnings --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
    # typecheck:
    #   run: pnpm run typecheck
